# SC Tools v10
Локальный веб-интерфейс для распаковки современных контейнеров .sc и .sctx (ZSTD, ASTC, ETC2).

## Установка
1. Установите системный компилятор: `sudo apt install astcenc`
2. Установите библиотеки: `pip install -r requirements.txt`
3. Запустите: `python app.py`
