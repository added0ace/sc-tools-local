# SC Tools Pro
Мощный локальный сервер для полной распаковки, декодирования текстур (ASTC/ETC2/RGBA) и обратной сборки контейнеров .sc / .sctx.

## Установка в Termux / Linux:
1. `sudo apt update && sudo apt install -y astcenc`
2. `pip install -r requirements.txt`
3. `python app.py`
